"""
arquivos
 - Quando abrimos um arquivo para leitura, se não existe, ocorre erro.
 - Quando abrimos um arquivo para leitura, se não existe, um novo é criado.
 - Quando abrimos um arquivo para escrita, se existe, é sobrescrito com o novo.
"""
str1 = 'python'
print(str1.center(15, '*'))

str2 = 'abababadaadbbaccabc'
print(len(str2))
print(str2.count('ab', -17, -1))
